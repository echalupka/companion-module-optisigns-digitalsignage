'use strict'

// Upgrade scripts run in order when a user upgrades to a newer module version.
// Add a new function here for each version that requires config/action/feedback migration.
module.exports = []
